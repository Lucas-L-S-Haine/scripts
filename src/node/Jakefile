const { desc, task } = require("jake");
const { runList, runLink, runUnlink } = require("./lib/jake_actions");

task("default", ["link"]);

desc("create symbolic links at target directory");
task("link", runLink);

desc("remove symbolic links");
task("unlink", runUnlink);
